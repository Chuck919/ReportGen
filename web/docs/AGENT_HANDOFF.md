# Agent Handoff

**Canonical handoff is the repo-root [`AGENTS.md`](../../AGENTS.md).** This file is a short pointer only.

```powershell
cd web
npx tsx scripts/benchmark-ui-session.ts balanced
npx tsx scripts/benchmark-ui-upload-routes.ts balanced
```

Production: OVH VPS (`https://reportgen.duckdns.org`).
