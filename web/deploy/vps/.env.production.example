# VPS production — copy to .env.production on the server.
# Do NOT set VERCEL or NEXT_PUBLIC_VERCEL=1 — UI uses local fast / balanced / thorough.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

FREE_OCR_MODE=balanced
FREE_OCR_WORKERS=2
FREE_OCR_MAX_WORKERS=2
FREE_OCR_TIMEOUT_MS=1200000
